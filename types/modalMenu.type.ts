export interface ModalMenu {
    to: {
        marginRight: string;
        opacity: number
    };
    from: {
        marginRight: string;
        opacity: number
    },
    config: {
        duration: number
    }
}